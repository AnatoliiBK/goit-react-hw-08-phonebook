export const selectLoading = state => state.contactsUser.contacts.loading;

export const selectFilter = state => state.contactsUser.filter;

export const selectAllContacts = state => state.contactsUser.constacts.items;
